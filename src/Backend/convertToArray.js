export const convertToArray = ({ className, placeHolder, value, onChange }) => {
  if (!array) {
    let array = []
  }

  const newObject = {
    className,
    placeHolder,
    value,
    onChange,
  }

  const newArray = array.push({ newObject })

  return newArray
}
